<nav class = "nav_container">
    <ul class = "nav">   
        <li>
            <%= link_to "Home", articles_path %>
        </li>

        <li>
            <%= link_to "New Article", new_article_path %>
        </li>

        <li>
            <%= link_to "New Article upload", new_upload_path %>
        </li>


         <% if current_user %>
             <li>
                <%= form_with(url: logout_path, method: :delete, class: "sair-form") do %>
                <%= submit_tag "Sair", class: "sair-button" %>
                <% end %>
            </li>
            <li>
                <%= link_to "Perfil", user_path(current_user) %>
            </li>
            <% else %>
            <li>
                <%= link_to "Login", login_path %>
            </li>
            <li>
                <%= link_to "Cadastro", register_path %>
            </li>
        <% end %>

        <li>
            <%= form_tag(articles_path, method: "get", class: "navbar-form navbar-left") do %>
                <div class="form-group">
                    <%= text_field_tag :tag_name, params[:tag_name], class: "form-control", placeholder: "Pesquisar por tag" %>
                </div>
                <%= submit_tag "Pesquisar", class: "btn btn-default" %>
            <% end %>
        </li>


    </ul>

</nav>